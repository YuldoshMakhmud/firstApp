import 'package:flutter/material.dart';

class DetailsPage extends StatelessWidget {
  // Your DetailsPage implementation
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Details Page'),
      ),
      body: Center(
        child: Text('Details here'),
      ),
    );
  }
}